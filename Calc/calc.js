// 練習
//let x = 10;
//let x = 'JavaScriptの変数';
//document.write(x);

// 計算問題
let before = 1000000;
let mul = 1.06 ** 10;
let after = before * mul;
let answer = after - before;
//document.write(answer);

// このコードは三角形の面積を求めるプログラミングです。
let w = 3;  // 三角形の底辺
let h = 10; // 三角形の高さ
let area = (w * h) / 2; // 三角形の面積の公式
//document.write(area);   // 面積を出力

/**
 * 円の面積を求める
 */
// 円の半径
let radius = 12

// 円の面積を求める公式
let circleArea = radius * radius * 3.14

// 円の面積を出力する
document.write(circleArea);